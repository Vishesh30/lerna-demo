module.exports.add = function(x,y){
    return x + y;
}

module.exports.addSquares = function(x,y){
    return (x*x) + (y*y);
}

module.exports.addCube = function(x,y){
    return (x*x*x) + (y*y*y);
}